# Upstream pin

- **Source:** https://github.com/g1879/DrissionPage.git
- **Absorbed commit (was nested git HEAD):** `6957a2858f58a218089be4fd7d85f22c1d496326`
- **Note:** Nested `.git` removed so this monorepo tracks library sources directly (editable install still works via `pip install -e ./DrissionPage`).
